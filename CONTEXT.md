# AI Foundation

AI Foundation 为 Halo 插件提供 Provider-neutral 的模型能力，并由管理员配置 Provider、Model 及其参数映射。

## Language

**模型参数目录（Model Parameter Catalog）**:
Provider-neutral 模型参数定义的权威集合；包含参数身份、适用模型类型及展示元数据，但不包含 Provider 或 Model 的映射选择，也不包含运行时参数值。
_Avoid_: 参数映射目录、模板注册表
