## No Spec-Level Changes

This change is a pure UI implementation fix that does not introduce new capabilities or modify existing spec-level requirements. The backend already exposes `supportedEndpointTypes` via `ProviderTypeInfo`; the frontend was simply not consuming it. No specification updates are required.
